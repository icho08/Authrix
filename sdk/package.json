{
  "name": "authrix-sdk",
  "version": "1.1.7",
  "description": "Authrix - The only authentication SDK you'll ever need for your React applications.",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "keywords": [
    "authentication",
    "auth",
    "react",
    "typescript",
    "sdk",
    "jwt",
    "hooks"
  ],
  "author": "icho08",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/icho08/authrix-sdk"
  },
  "homepage": "https://authrix.chhabi.xyz",
  "bugs": {
    "url": "https://github.com/icho08/authrix-sdk/issues"
  },
  "scripts": {
    "build": "bun run build:clean && bun run build:bundle && bun run build:types",
    "build:clean": "rm -rf dist",
    "build:bundle": "bun build src/index.ts --outdir dist --target browser --format esm --minify --splitting --outfile index.js",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir dist",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "dev": "bun run src/index.ts",
    "prepublishOnly": "bun run build",
    "prepack": "bun run build",
    "postpack": "bun run build:clean"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "authrix-sdk": "^1.0.1"
  }
}
